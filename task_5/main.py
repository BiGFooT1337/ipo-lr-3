# Используем число от пользователя  
number = float(input("Введите число: "))

# Используем вложенное тернарное выражение 
result = ("Положительное" if number > 0 else 
          "Отрицательное" if number < 0 else
          "Нейтральное")

# Выводим результат 
print(result)              